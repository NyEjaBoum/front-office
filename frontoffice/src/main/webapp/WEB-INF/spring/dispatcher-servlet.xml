<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans
           https://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/context
           https://www.springframework.org/schema/context/spring-context.xsd
           http://www.springframework.org/schema/mvc
           https://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!-- Charge le fichier de configuration -->
    <context:property-placeholder location="classpath:application.properties" />

    <!-- Active les annotations (@Controller, @Service, @Repository, etc.) -->
    <mvc:annotation-driven />

    <!-- Scanne TOUS les packages pour trouver les composants Spring -->
    <context:component-scan base-package="com.frontoffice" />

    <!-- Résolution des vues JSP -->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/views/" />
        <property name="suffix" value=".jsp" />
    </bean>

    <!-- Ressources statiques -->
    <mvc:resources mapping="/css/**"     location="/css/,classpath:/static/css/" />
    <mvc:resources mapping="/js/**"      location="/js/,classpath:/static/js/" />
    <mvc:resources mapping="/images/**"  location="/images/,classpath:/static/images/" />

</beans>
